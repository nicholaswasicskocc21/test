import time
import RPi.GPIO as IO

IO.setmode(IO.BOARD)
IO.setwarnings(false)
IO.setup(IO.OUT)

while True:
  Chan_List[40, 38]
  IO.output(Chan_List, IO.HIGH)
  time.sleep(5)
  
  IO.output(Chan_List, IO.LOW)
  time.sleep(5)
